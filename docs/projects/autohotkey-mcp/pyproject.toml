[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "autohotkey-mcp"
version = "0.2.0"
description = "MCP server for AutoHotkey v2 scriptlets: list, run, stop, source/metadata, AI generation (sampling), prefab UI cards, MCP prompts. Uses ScriptletCOMBridge and autohotkey-test repo as depot."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.4.4,<4",
    "fastapi>=0.95",
    "httpx>=0.24",
    "prefab-ui>=0.14.0",
    "uvicorn>=0.21",
    "hatchling>=1.29.0",
]

[project.optional-dependencies]
dev = ["pytest>=7", "pytest-asyncio>=0.21", "ruff>=0.8"]

[project.scripts]
autohotkey-mcp = "autohotkey_mcp.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/autohotkey_mcp"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-v"

[tool.ruff]
line-length = 100
target-version = "py312"
